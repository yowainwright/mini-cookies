{
  "name": "mini-cookies",
  "version": "0.0.1-beta.0",

  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  },
  "dependencies": {
    "@types/jest": "^27.4.0",
    "jest": "^27.4.7",
    "ts-jest": "^27.1.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "jest-environment-node"
  }
}
